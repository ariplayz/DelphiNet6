{
  "name": "delphinet6",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev:api": "pnpm --filter @delphinet/api dev",
    "dev:web": "pnpm --filter @delphinet/web dev",
    "build:api": "pnpm --filter @delphinet/api build",
    "build:web": "pnpm --filter @delphinet/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  }
}
